# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Outillage BMAD : l'installation locale et ce qu'elle produit. Régénérable,
# et propre au poste de travail — ce n'est pas du source du projet.
_bmad/
_bmad-output/

# Personal scratch files
todo.md

# Gessie reference bundle (referenced in code comments by byte offset, not built)
renderer.js

# Documentation de travail : relevés, spécifications reprises du simulateur
# d'origine, et maquettes de design (bundles HTML autonomes ~1,7 Mo chacun).
# Matériel de référence, pas du source — le port vit dans src/.
docs/

# Claude Code : réglages, compétences et agents installés localement.
.claude/
